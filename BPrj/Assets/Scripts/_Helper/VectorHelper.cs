/*
using UnityEngine;

/// <summary>
/// VectorHelper offers additional methods for working with UnityEngine.Vector2 data type.
/// </summary>
public static class VectorHelper
{
    /// <summary>
    /// Temporary Vector2 used to store values.
    /// </summary>
    private static Vector2 tempVector;

    /// <summary>
    /// Returns this vector with rounded coordinates.
    /// </summary>
    public static Vector2 Rounded(this Vector2 vector)
    {
        tempVector.Set(Mathf.Round(vector.x), Mathf.Round(vector.y));
        return tempVector;
    }
}
*/